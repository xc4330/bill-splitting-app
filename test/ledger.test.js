import Ledger from '../ledger.js'

describe('Ledger', () => {
    describe('parseNames', () => {
        test('should return list of names seperated by \\n', () => {
            expect(2 + 2).toBe(4);
        });
    })
})